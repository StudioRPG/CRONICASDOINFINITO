import { RacaTipo, ClasseTipo, Atributos } from './types';

// Placeholder imports - I will update the data files next to match these types.
import { RACAS } from './data/races';
import { CLASSES } from './data/classes';

export const ATRIBUTOS_INICIAIS: Atributos = {
    forca: 1,
    habilidade: 1,
    resistencia: 1,
    armadura: 1,
    poderDeFogo: 1
};
